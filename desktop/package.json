{
  "name": "clawleash-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "Desktop menubar companion for clawleash — bottom-right Allow/Deny toast + one-click phone handoff.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
